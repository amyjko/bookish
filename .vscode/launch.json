{
    // To use this debug configuration, first run this on the command line: node --inspect-brk node_modules/.bin/jest --runInBand
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "attach",
            "name": "Attach",
            "port": 9229
          }
    ]
}